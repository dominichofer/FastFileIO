[build-system]
requires = ["setuptools >= 64"]
build-backend = "setuptools.build_meta"

[project]
name = "fastfileio"
version = "0.0.1"
requires-python = ">= 3.10"
dependencies = [
    "matplotlib",
]

[tool.setuptools]
packages = ["fastfileio", "plot"]
